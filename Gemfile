# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in openapi_rspec.gemspec
gemspec

group :test do
  gem 'simplecov', require: false, platform: :mri
end
